# About

Shows how to abort a stdin reader.

**NB** This needs Deno 2.8.3+ due to https://github.com/denoland/deno/issues/34985.

## Usage

Execute the example:

```bash
deno run --allow-all main.ts
```
